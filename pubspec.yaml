name: footballhub
description: "A Flutter app for football fans"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.13.0
  cupertino_icons: ^1.0.8
  google_fonts: ^6.1.0
  animate_do: ^3.3.4
  flutter_animate: ^4.5.0
  http: ^1.2.1
  provider: ^6.1.2
  intl: ^0.19.0
  cached_network_image: ^3.3.1
  flutter_spinkit: ^5.2.0
  cloud_firestore: ^5.6.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true